#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{

    long long int a, b, result;
    scanf("%lld %lld", &a, &b);

    result = a * b;
    printf("%lld", result);

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    return 0;
}
